# v0 runtime files (should only exist in preview, not in git)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Common ignores
node_modules/
.next/
.env*.local
.DS_Store
.vercel
tsconfig.tsbuildinfo

# Certificate / key material — never commit these. The Graph mail certificate
# (see scripts/graph-cert-check.mjs) is handled locally and lives only in the
# server's environment variables.
.env-snippet.txt
*.pfx
*.p12
*.pem
*.cer
*.crt
*.key
